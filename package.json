{
  "name": "watchpreview",
  "version": "1.0.0",
  "private": true,
  "description": "Framework-agnostic static preview server with file-watch reload and single-instance idempotency.",
  "scripts": {
    "build": "node scripts/build.js",
    "test:contract": "powershell -ExecutionPolicy Bypass -File test/integration.ps1 -Exe .build/watchpreview.exe"
  }
}