# 设计文档（内部开发 / 维护）

面向 watchpreview 内部开发者：架构、机制、边界决策、回归验证。集成使用请看 `README.md`。

## 范式与进程模型

watchpreview 的价值 = **一个进程、一份配置，接管"源码监听→编译→dist服务→浏览器实时预览"整条链路**。

上层框架只见 wrapper（`preview --config` 命令）：stdout 第一行是 URL，命令立刻返回。真正的 HTTP server 是 wrapper fork 出的一个 detached 子进程（独立生命周期）。

```
wrapper（用户调用的进程）
  ├─ loadConfig（解析 JSON 或隐式默认：serveRoot=cwd）
  ├─ 幂等判断：状态文件两层核验（PID + HTTP /control/status）→ 健康复用 → 输出 URL，立即返回
  └─ 陈旧/无实例 → fork --foreground 子进程
        ├─ net.Listen("127.0.0.1:0") 一次绑定
        ├─ 写 <id>.json 状态文件
        ├─ 起 watcher（按配置决定监听源码或 dist）
        └─ 起 http.Server 直到 stop/信号
```

幂等键：规范化后的 `serveRoot`（`filepath.Abs` + `EvalSymlinks`）。id = sha256(canonical root) 前 16 位 hex。

---

## 源码结构（`src/`，package main）

| 文件 | 职责 |
|---|---|
| `main.go` | 子命令分发（preview / status / stop，含 `--version`） |
| `config.go` | `Config`（`serveRoot`/`watch`/`exclude`/`onChangeCommand`）+ `loadConfig`（文件/隐式/校验/路径解析）+ `sourceConfigHash` |
| `cmd_preview.go` | preview wrapper 幂等 & fork + foreground server + `configMismatches`，版本变量 `version` |
| `cmd_stop.go` | stop（HTTP 优雅停止 + 陈旧降级路径，防误杀） |
| `cmd_status.go` | status（打印实例 JSON 或 not running） |
| `server.go` | HTTP handler：静态服务、SSE reload、控制端点（固定 404，无 fallback） |
| `watch.go` | fsnotify watcher：多根监听、exclude 前缀过滤、onChangeCommand 编译管道、防抖 |
| `instance.go` | `Instance` 结构与状态文件读写（含 `sourceHash`） |
| `id.go` | `canonicalizeRoot` / `computeID` / `stateDir` 回退链 / 文件路径工具 |
| `pid.go` | PID 存活检查、进程终止/强杀 |
| `reload.go` | 刷新事件转发（ReloadHub） |
| `util.go` | `randomToken` |
| `startup_error.go` | `.err` 诊断文件读写删 |
| `detach_windows.go` / `detach_unix.go` | 平台化 detached 启动 |
| `noconsole_windows.go` / `noconsole_other.go` | `hideConsole`：Windows 下给编译 cmd 加 `CREATE_NO_WINDOW`，unix 空实现 |

---

## 配置模型

只保留与范式直接相关的字段，行为固定（无参数暴露）：

| 字段 | 类型 | 说明 |
|---|---|---|
| `serveRoot` | string | 被服务的 dist 目录（**幂等键**） |
| `watch` | []string | 源码监听根（递归）；有 `onChangeCommand` 时生效 |
| `exclude` | []string | 路径前缀过滤，只作用于当前监听树 |
| `onChangeCommand` | string | 编译命令，非空启用"源变→编译→刷新"管道 |

**固定行为（硬编码，不暴露为参数）：**

- 监听地址 `127.0.0.1`（不可 `0.0.0.0`）；端口自动分配
- 无 fallback 路由（html-suffix/spa）：静态文件找不到即 404
- 不自动开浏览器
- 编译命令工作目录 = config 文件所在目录（`Config.dir`，非 JSON 字段）

路径规则：所有路径相对 config 文件所在目录；绝对路径直接采用（`joinFromBase`）。隐式模式（无 `--config`）：serveRoot = cwd，无 watch/exclude/onChangeCommand。

---

## 监听与编译管道（watch.go）

- **多根监听**：有 `onChangeCommand` 时监听 `watch` 集合；无则监听 `serveRoot`。**刻意不监听 serveRoot（有编译时）**——避免编译产物写回 dist 再触发再编译的环路。
- **exclude**：路径前缀+边界匹配（`path == e` 或 `path` 以 `e + /` 开头）；目录级 `SkipDir`、事件级过滤。内置忽略 `.git`/`node_modules`/点开头目录。
- **防抖**：1000ms（1 秒），合并批量写；fire 后置入 goroutine，不阻塞事件循环。
- **编译执行（Windows）**：整条命令**原样写入临时 `.watchpreview-*.cmd` 脚本**（UTF-8 无 BOM，首两行 `@echo off` + `chcp 65001 >nul`），再以 `cmd /D /C <相对文件名>` 执行，`cmd.Dir` = config 所在目录，跑完 `defer` 删除临时脚本。**为什么不直接 `cmd /C <命令>`**：
  - Go 拼命令行时把参数里的 `"` 转成 `\"`，而 `cmd.exe` 及批处理是解析例外、不认反斜杠引号（Go 官方文档明言），任何带引号的命令串都会碎。写成批处理后命令按行原生解析，引号、中文路径都正确。
  - 中文 Windows 上 cmd 默认按 ANSI(GBK) 读批处理文件，UTF-8 中文会乱码，故首行先 `chcp 65001 >nul` 切到 UTF-8（该两行必须先于任何非 ASCII 内容）。
  - 编译命令由 `hideConsole` 以 `CREATE_NO_WINDOW` 在隐藏控制台运行（watchpreview 自身以 `DETACHED_PROCESS` 启动、无控制台，若不设标志 cmd 会弹出新窗口闪一下）。
  - 脚本用无空格随机相对文件名传参，避开"`cmd /C` 带引号尾随路径"的解析坑。
- **编译执行（macOS/Linux）**：`sh -c <命令>`，60s 超时，`cmd.Dir` = config 所在目录。exit 0 → reload；非 0/超时 → 不刷新，stderr 报错。
- **串行**：`compileMu` 保证编译不并行。

---

## 状态文件

位置：`%LOCALAPPDATA%/watchpreview`（Win）、`~/Library/Caches/watchpreview`（macOS，可能被清理）、`~/.cache/watchpreview`（Linux）。回退链：`UserCacheDir → UserConfigDir → UserHomeDir → TempDir`。

每个实例一个 `<id>.json`：

```json
{
  "id": "92e49140f70fc937",
  "root": "C:\\...\\dist",
  "pid": 1760,
  "port": 26007,
  "url": "http://127.0.0.1:26007/",
  "token": "8c83...",
  "sourceHash": "a1b2c3..."
}
```

---

## 幂等与陈旧状态判定

状态文件存在 ≠ 实例还活着。两层校验：

1. `pidAlive(PID)` → 失败 → 陈旧
2. GET `/control/status`，800ms 超时 → 失败/返回 id/root 不匹配 → 陈旧

| 命令 | 陈旧处理 |
|---|---|
| preview | 静默清理，自动重启，用户无感 |
| status | 清理，报告 not running |
| stop | 优雅停止；降级：PID 存活且身份匹配 → 强杀；PID 存活但身份不匹配 → 不杀（防误杀复用 PID），提示手动确认；PID 消失 → 只清文件 |

边界（刻意从简）：
- **并发 race**：极短时间同时 preview 同 serveRoot，可能各起一个实例；本地工具顺序执行概率低，`stop` 可清。
- **挂死进程**：`stop` 无法确认 PID 时选择不杀，只清状态文件。
- **就绪间隙**：instance.json 写出到 Serve() 起来间有极短窗口；守卫脚本用 500ms 间隔规避。

---

## 启动失败诊断（.err 链路）

foreground 子进程无可见控制台，失败时：
1. 写 `<id>.err`（同状态目录，`0600`），exit 非 0
2. wrapper 轮询 instance.json（最多 5s），期间读到 `.err` 立即转述到 stderr 并 exit 非 0
3. 读即删，启动前先清残留

保证"失败原因在 stderr、不丢"。

---

## HTTP 控制端点

| 路径 | 方法 | 鉴权 | 用途 |
|---|---|---|---|
| `/_watchpreview/reload` | GET | 无 | SSE，浏览器订阅刷新 |
| `/_watchpreview/control/stop` | POST | Token | 优雅停止 |
| `/_watchpreview/control/status` | GET | 无 | 返回 id/root，供身份核对 |

---

## 实例行为告警（configMismatches）

幂等键只有 serveRoot。`watch`/`exclude`/`onChangeCommand` 变更不触发重建，靠 `sourceHash`（启动时对这三项取摘要）比对，不一致时 stderr 提示 `run 'stop' then 'preview' again to apply`。旧实例无 sourceHash 时跳过（兼容）。

---

## 构建与版本注入

```bash
go build -o dist/watchpreview-{GOOS}-{GOARCH}[.exe] ./src
```

版本号 `var version`（必须是变量）由 `.goreleaser.yaml` 的 `-X main.version={{.Version}}` 注入；本地构建为 `dev`。

---

## 契约守卫测试（回归入口）

改完二进制必须跑。断言 6 个场景（26 项检查）：

1. 显式 config：exit 0 + 恰一行 http url
2. 幂等复用：二次调用同 URL
3. status 含 url；stop exit 0 + "stopped"；stop 后 status not running
4. **隐式 cwd**：同上四步
5. **exclude**：watch 内文件触发编译（marker 产生）；exclude 内文件**不触发**
6. **onChangeCommand 失败**：不刷新（marker 不产生）、进程仍 alive
7. **sourceHash 告警**：config 变更后复用 → stderr 含 "run 'stop'"
8. **引号路径**：onChangeCommand 含带引号的含空格路径 → marker 正确产生（回归 `cmd /C` 引号坑）
9. **中文字符路径**：onChangeCommand 中中文路径 → marker 正确产生（回归 chcp/UTF-8 坑）

```bash
go build -o .build/watchpreview.exe ./src
powershell -ExecutionPolicy Bypass -File test/integration.ps1 -Exe .build/watchpreview.exe
# → ALL CONTRACT CHECKS PASSED (32 checks)
```

守卫脚本注意（维护者）：

- 脚本与配置 JSON 保持**纯 ASCII**（PS 5.1 无 BOM 按 ANSI 解析中文注释会干扰）；中文测试路径用 `[char]0x4E2D` 码点拼接
- `Expand-Config` 写 JSON 用**无 BOM UTF-8**（BOM 会让 `json.Unmarshal` 报 `invalid character 'ï'`）
- **不要用 PS5.1 的 `2> $errFile` 捕获 stderr**：它写入的是格式化错误记录、且会按控制台宽度 wrap 长行（"stop" 会被拆成 `st`+CRLF+`op'`，字串断言失真）；`Invoke-WP` 用 `.NET Process` 原始捕获 stdout/stderr
- step 间隔 500ms 规避"instance.json 写出到 Serve() 起来"的就绪间隙
- onChangeCommand 的 marker 路径现在支持引号与中文（框架写临时批处理 + chcp），仍建议用简单路径减少干扰
- 脚本覆盖 `$env:LOCALAPPDATA` 隔离状态目录

---

## 安全设计

- 固定 `127.0.0.1`；不可 `0.0.0.0`
- `/control/stop` 需随机 token（每次启动生成，存状态文件，仅本机可读）
- 静态文件服务有路径越界校验，`../` 无法逃出 root