# 设计文档（内部开发 / 维护）

本文件面向 watchpreview 的**内部开发者与维护者**：架构、机制、边界决策，以及"改了代码之后如何验证"。想要了解**怎么集成使用**，请看 `README.md` 或 `skill/SKILL.md`（AI agent 速查）。

## 概览与进程模型

`watchpreview preview --root <dir>` 不是一个常驻进程，而是一个 **wrapper**：

```
wrapper（用户直接调用的进程）
  ├─ 幂等判断：读状态文件并两层核验，健康 → 复用，输出 URL，立即返回
  ├─ 陈旧/无实例 → fork 自身为 --foreground 子进程然后轮询等待
  └─ 子进程（detached，独立生命周期）
        ├─ net.Listen 一次绑定端口（消除"先找空闲端口后绑定"的 TOCTOU）
        ├─ 写 <id>.json 状态文件
        ├─ 起 watcher（文件变化 → SSE 通知浏览器刷新）
        └─ 起 http.Server 直到 stop/信号
```

上层框架从来只见 wrapper：命令立即返回、stdout 第一行是 URL。真正的 HTTP server 是 wrapper fork 出的 detached 子进程，不占用调用方的终端/进程句柄。幂等键只有一个——规范化的 `--root`（`canonicalizeRoot` 用 `filepath.Abs` + `EvalSymlinks` 归一化，保证相对路径、符号链接、不同 cwd 都算出同一个目录）；id = sha256(规范化 root) 前 16 位 hex。

## 目录结构与模块职责

所有代码在 `src/` 下，package main：

| 文件 | 职责 |
|---|---|
| `main.go` | 子命令分发（preview/status/stop） |
| `cmd_preview.go` | `preview`：wrapper 幂等&拉起 + foreground server + 参数比对告警，版本号 `v1.0.0` |
| `cmd_stop.go` | `stop`：HTTP 优雅停止 + 降级路径（防误杀） |
| `cmd_status.go` | `status`：打印实例信息 / `preview is not running` |
| `id.go` | `canonicalizeRoot` / `computeID` / `stateDir` 回退链 / `instanceFilePath` / `startupErrPath` |
| `instance.go` | `Instance` 结构（含 host/fallback/ignore 字段）与状态文件读写 |
| `startup_error.go` | `<id>.err` 诊断文件的写/读/删 |
| `server.go` | HTTP handler：静态服务、SSE reload、控制端点 |
| `watch.go` | fsnotify watcher 与 deps 组件（reload hub 等） |
| `reload.go` | 刷新事件转发 |
| `util.go` | 通用小工具 |
| `pid.go` | PID 存活检查、进程终止/强杀 |
| `detach_windows.go` / `detach_unix.go` | 平台化的 detached 启动（Windows 用 `DETACHED_PROCESS`） |

## 状态目录与状态文件 schema

状态目录按平台惯例选择，逐级回退，`0700` 权限，无需提权：

| 平台 | 位置 |
|---|---|
| Windows | `%LOCALAPPDATA%\watchpreview` |
| macOS | `~/Library/Caches/watchpreview`（可能被系统清理；丢失即"陈旧状态"，下次 preview 自动重启） |
| Linux | `$XDG_CACHE_HOME`（默认 `~/.cache`）下的 `watchpreview` |

回退链：`os.UserCacheDir()` → `UserConfigDir` → `UserHomeDir` → `os.TempDir()`。不再使用旧的 `~/.watchpreview`（见"从旧版升级"）。

每个实例一个 `<id>.json`（`Instance` 结构）：

```json
{
  "id": "92e49140f70fc937",
  "root": "C:\\...\\dist",
  "pid": 1760,
  "port": 26007,
  "url": "http://127.0.0.1:26007/",
  "token": "8c83...（随机，控制端点鉴权用）",
  "host": "127.0.0.1",
  "fallback": "none",
  "ignore": ""
}
```

`host`/`fallback`/`ignore` 为 v1.0.0 新增字段（`omitempty`），旧文件仍可解析，缺失字段不参与比对（见"参数比对告警"）。

## 幂等与陈旧状态判定

**状态文件的存在不代表实例还活着**：进程 `kill -9`/断电会留残骸；PID 可能被系统复用给无关进程；进程可能挂死（HTTP 不响应）。因此存活判定是**两层校验**：

```
checkInstanceAlive(inst):
  1. pidAlive(inst.PID) == false                → 陈旧
  2. GET {inst.URL}/_watchpreview/control/status，800ms 超时
     - 请求失败/超时                            → 陈旧（进程可能挂死）
     - 返回的 id/root 对不上                    → 陈旧（PID 被复用给了别的进程）
  3. 两层都过                                  → 存活
```

各命令的降级行为：

| 命令 | 发现陈旧状态 |
|---|---|
| `preview` | 静默清理状态文件，重启新实例，用户无感 |
| `status` | 清理状态文件，报告 `preview is not running` |
| `stop` | 优雅停止失败后：PID 存活**且**能靠控制端点核对 id/root 身份 → 强制终止；PID 存活但核对不上（疑似 PID 被复用）→ **不杀 PID**，只清状态文件并提示手动确认；PID 已不存在 → 只清状态文件。两种情况最终都报 `preview stopped` |

### 已知边界情况（刻意从简，未处理）

- **并发 race**：两个 `preview --root x` 极短时间同时执行，可能都判定"无活实例"各自拉起 server。本地开发工具单用户顺序执行概率低、代价可控（浪费一个端口 + 孤儿进程，`stop` 可清）。若未来要支持并发频繁场景（CI job 并行同目录），需在 `instanceFilePath` 写入前加文件锁（`O_CREATE|O_EXCL`）。
- **挂死进程只能部分回收**：`stop` 只在控制端点能核对上身份时才强杀；进程挂死到连控制端点都不响应时，无法确认 PID 是否还是本实例，**为防误杀被复用的无关进程选择不杀**，仅清状态文件并提示手动确认。

## 启动失败诊断（.err 传递链路）

foreground 子进程 detached 无可见控制台，直接写 stderr 等于丢诊断。机制：

1. 子进程启动失败（端口绑定失败、状态文件写失败）→ `reportStartupError` 写 `<id>.err`（同状态目录，`0600`），退出码非 0。
2. wrapper 轮询等待 instance.json（50 × 100ms = 5s 超时），期间一旦读到 `.err` 立即向 stderr 转述 `watchpreview: server failed to start: <原因>` 并以非 0 退出，不用干等超时。
3. 读到即删，启动成功前也先清残留，避免陈旧错误文件误导下一次启动。

这保证集成契约里"失败时原因在 stderr、不会丢"这一条成立。

## HTTP 控制端点

| 路径 | 方法 | 鉴权 | 用途 |
|---|---|---|---|
| `/_watchpreview/reload` | GET | 无 | SSE，浏览器订阅刷新事件 |
| `/_watchpreview/control/stop` | POST | `X-WatchPreview-Token` header | 优雅停止 |
| `/_watchpreview/control/status` | GET | 无（只暴露非敏感 id/root） | 陈旧状态两层校验的 PID 身份核对 |

## 参数比对告警（configMismatches）

幂等键只有 root：`--host/--port/--fallback/--ignore` 只在首次启动时生效，复用已有实例时被忽略，仅做可观察性提示（stderr 告警），URL 照旧。两条宽容规则防误报：

- 请求 `port == 0`（自动分配）不代表明确意图，不比对该项；
- 已有实例的 host/fallback/ignore 为空（旧版状态文件缺字段）时无法还原意图，跳过。

## 构建与交叉编译

```bash
go build -o dist/watchpreview-<os>-<arch> ./src
```

按目标平台交叉编译，产物命名建议 `watchpreview-{GOOS}-{GOARCH}[.exe]`，上层工具按 `process.platform`/`process.arch` 选择。模块名 `github.com/doc-war/watchpreview`，仓库已提交 `go.sum`，全新 clone 即可构建。

## 契约守卫测试（回归入口）

改完二进制必须跑守卫，验证"集成契约没被破坏"。断言：

1. 首次启动 exit 0，stdout **恰一行**且以 `http://` 开头；
2. 二次调用幂等复用**同一个 URL**，仍单行；
3. `status` exit 0 且输出包含该 URL；
4. `stop` exit 0 且输出含 `preview stopped`。

```bash
go build -o .build/watchpreview.exe ./src
powershell -ExecutionPolicy Bypass -File test/integration.ps1 -Exe .build/watchpreview.exe
# → ALL CONTRACT CHECKS PASSED
```

守卫脚本细节（维护者注意）：

- **文件必须保持纯 ASCII**。PowerShell 5.1 把无 BOM 的 `.ps1` 按 ANSI 代码页解析，非 ASCII 注释会在部分机器上干扰解析（曾导致 `[bool]` 断言参数收到 `Object[]`、`$LASTEXITCODE` 被吞等诡异失败）。
- 断言函数 `Assert-Contract` 的 `Condition` 参数为 `[object]`，内部显式 `[bool]` 转换——`-match` 对数组返回匹配行数组而非 bool，直接传 `[bool]` 参数会失败。
- step1 → step2 之间保留 400ms 就绪间隙：instance.json 刚写出到 `Serve()` 起来之间有极短窗口，立即二次调用可能把新实例误判为陈旧而重建。
- 脚本通过覆盖 `$env:LOCALAPPDATA` 隔离状态目录，不污染真实 `%LOCALAPPDATA%\watchpreview`。

## 安全设计

- 默认只监听 `127.0.0.1`；改 `0.0.0.0` 前确认可信网络环境（如自有局域网真机预览），该模式下打印的 `http://0.0.0.0:...` 字面值在真机不可达，需替换为局域网 IP。
- `/control/stop` 需要随机 token（每次启动生成，存状态文件中，仅本机可读）；`/control/status` 无鉴权因为只暴露非敏感 id/root。
- 静态文件服务做了路径越界校验，`../` 无法逃出 root。